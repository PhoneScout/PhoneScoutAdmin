<UserControl x:Class="PhoneScoutAdmin.Views.CameraSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhoneScoutAdmin.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT SIDE -->
        <StackPanel Grid.Column="0" Background="#EAEAEA">

            <Button Grid.Row="2" Content="Add camera" Margin="0,10,0,10" FontSize="16" Width="180" Padding="5" Background="White" Foreground="Green" BorderBrush="#80A4CB" BorderThickness="3" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding AddCameraCommand}"/>

            


            <ListBox ItemsSource="{Binding Cameras}"
                     SelectedItem="{Binding SelectedCamera}"
                     DisplayMemberPath="Name"
                     Margin="10"/>

            <Button Grid.Row="2" Content="Remove camera" Margin="0,10,0,10" FontSize="16" Width="180" Padding="5" Background="White" Foreground="Red" BorderBrush="#80A4CB" BorderThickness="3" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding RemoveCameraCommand}"/>

        </StackPanel>

        <!-- RIGHT SIDE -->
        <ContentControl Grid.Column="1"
                        Content="{Binding SelectedCamera}"/>

    </Grid>
</UserControl>
