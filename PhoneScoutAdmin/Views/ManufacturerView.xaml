<UserControl x:Class="PhoneScoutAdmin.Views.ManufacturerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="LightGray">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <WrapPanel Grid.Row="0" Orientation="Horizontal" 
Grid.ColumnSpan="2">

            <TextBlock Text="Manufacturer Name:" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5,0,10,0" FontWeight="Bold"/>

            <TextBox Width="150"
 Height="20"
 Margin="5"
 Background="White"
 Text="{Binding ManufacturerNameFilter, UpdateSourceTrigger=PropertyChanged}" />



        </WrapPanel>

        <!-- ===================== -->
        <!-- MANUFACTURER LIST -->
        <!-- ===================== -->

        <DataGrid Grid.Row="1"
                  Grid.Column="0"
                  ItemsSource="{Binding ManufacturersView}"
                  SelectedItem="{Binding SelectedManufacturer}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="5" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Manufacturer Name" Binding="{Binding manufacturerName}" />
                <DataGridTextColumn Header="Manufacturer URL" Binding="{Binding manufacturerUrl}" />
                <DataGridTextColumn Header="Manufacturer Email" Binding="{Binding manufacturerEmail}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- ===================== -->
        <!-- EDIT PANEL -->
        <!-- ===================== -->

        <GroupBox Grid.Row="1"
                  Grid.Column="1"
                  Margin="5"
                  Padding="25"
                  Header="Modify the manufacturer details"
                  BorderBrush="#2300B3"
                  BorderThickness="2"
                  VerticalAlignment="Center">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Name -->
                <StackPanel Grid.Row="0"
       Grid.Column="0"
       Orientation="Horizontal"
       VerticalAlignment="Center">



                    <TextBlock Text="ℹ"
          FontSize="16"
Foreground="#2300B3"
          Cursor="Hand"
          VerticalAlignment="Center" ToolTipService.InitialShowDelay="500">

                        <TextBlock.ToolTip>
                            <ToolTip>
                                Displays the name of the manufacturer
                            </ToolTip>
                        </TextBlock.ToolTip>

                    </TextBlock>
                    <TextBlock Text="Name:"
          Margin="5,5,2,5"
          FontSize="16"
          VerticalAlignment="Center"/>
                </StackPanel>
                
                


                <TextBox Grid.Row="0"
                         Grid.Column="1"
                         Width="180"
                         Margin="5"
                         Text="{Binding ManufacturerName, UpdateSourceTrigger=PropertyChanged}" />

                <!-- URL -->
                <StackPanel Grid.Row="1"
       Grid.Column="0"
       Orientation="Horizontal"
       VerticalAlignment="Center">



                    <TextBlock Text="ℹ"
          FontSize="16"
Foreground="#2300B3"
          Cursor="Hand"
          VerticalAlignment="Center" ToolTipService.InitialShowDelay="500">

                        <TextBlock.ToolTip>
                            <ToolTip>
                                Displays the manufacturer's official website's URL.
                            </ToolTip>
                        </TextBlock.ToolTip>

                    </TextBlock>
                    <TextBlock Text="URL:"
          Margin="5,5,2,5"
          FontSize="16"
          VerticalAlignment="Center"/>
                </StackPanel>


                <TextBox Grid.Row="1"
                         Grid.Column="1"
                         Width="180"
                         Margin="5"
                         Text="{Binding ManufacturerURL, UpdateSourceTrigger=PropertyChanged}" />

                <!-- Email -->
                <StackPanel Grid.Row="2"
       Grid.Column="0"
       Orientation="Horizontal"
       VerticalAlignment="Center">



                    <TextBlock Text="ℹ"
          FontSize="16"
Foreground="#2300B3"
          Cursor="Hand"
          VerticalAlignment="Center" ToolTipService.InitialShowDelay="500">

                        <TextBlock.ToolTip>
                            <ToolTip>
                                Displays the manufacturer's e-mail address.
                            </ToolTip>
                        </TextBlock.ToolTip>

                    </TextBlock>
                    <TextBlock Text="E-mail:"
          Margin="5,5,2,5"
          FontSize="16"
          VerticalAlignment="Center"/>
                </StackPanel>


                <TextBox Grid.Row="2"
                         Grid.Column="1"
                         Width="180"
                         Margin="5"
                         Text="{Binding ManufacturerEmail, UpdateSourceTrigger=PropertyChanged}" />

                <!-- Save -->
                <Button Grid.Row="3"
                        Grid.ColumnSpan="2"
                        Margin="10,15,10,0"
                        Padding="5"
                        FontSize="18"
                        Background="WhiteSmoke"
                        BorderBrush="#68F145"
                        BorderThickness="3"
                        Content="Save changes"
                        Command="{Binding SaveManufacturerCommand}" />
            </Grid>
        </GroupBox>

        <!-- ===================== -->
        <!-- DELETE -->
        <!-- ===================== -->

        <StackPanel Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="10">

        </StackPanel>

    </Grid>
</UserControl>
