<UserControl x:Class="PhoneScoutAdmin.Views.ColorSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhoneScoutAdmin.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT SIDE -->
        <StackPanel Grid.Column="0" Background="#EAEAEA">

            <Button Content="Add Camera"
                    Command="{Binding AddColorCommand}"
                    Margin="10"/>

            <ListBox ItemsSource="{Binding Colors}"
                     SelectedItem="{Binding SelectedColor}"
                     DisplayMemberPath="Name"
                     Margin="10"/>

            <Button Content="Delete Camera"
                    Command="{Binding RemoveColorCommand}"
                    Margin="10"/>

        </StackPanel>

        <!-- RIGHT SIDE -->
        <ContentControl Grid.Column="1"
                        Content="{Binding SelectedColor}"/>

    </Grid>
</UserControl>
